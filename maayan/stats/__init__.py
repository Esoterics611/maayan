"""Knowledge-base health: a steward's-eye view of the corpus (Prompt 19).

Read-only aggregation across the SQLite stores (+ an optional Qdrant point count):
chunks by source and by book, contributions by author, developments by status,
retractions, threads, terms. This is the reviewer's dashboard — what to retract, and
how the asset is growing.
"""
